<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema MaterialComponents adequado para Wear OS -->
    <style name="Theme.Wearable.Material" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">#02244B</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondary">#44F09D</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/black</item>
    </style>

    <!-- Tema usado apenas no SplashScreen -->
    <style name="MainActivityTheme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Wearable.Material</item>
    </style>

</resources>